{% import "rally.helpers" as rally %}
{
  "name": "logging-frozen-tier-esql",
  "description": "Runs queries when indices are already loaded",
  "parameters": {
    "generate-data": false
  },
  "schedule": [
    {
      "name": "check-cluster-health",
      "operation": {
        "operation-type": "cluster-health",
        "index": "_all",
        "request-params": {
          "wait_for_status": "{{cluster_health | default('green')}}",
          "wait_for_no_relocating_shards": "true"
        },
        "retry-until-success": true
      }
    },
    {
      "operation": "apache_match_esql_query",
      "clients": 1,
      "warmup-iterations": 20,
      "iterations": 100,
      "tags": ["esql", "search"],
      "bulk_start_date": "{{ p_frozen_esql_query_min_date }}",
      "bulk_end_date": "{{ p_frozen_esql_query_max_date }}"
    },
    {
      "operation": "kafka_match_esql_query",
      "clients": 1,
      "warmup-iterations": 20,
      "iterations": 100,
      "tags": ["esql", "search"],
      "bulk_start_date": "{{ p_frozen_esql_query_min_date }}",
      "bulk_end_date": "{{ p_frozen_esql_query_max_date }}"
    },
    {
      "operation": "qstr_esql_query",
      "clients": 1,
      "warmup-iterations": 20,
      "iterations": 100,
      "tags": ["esql", "search"],
      "bulk_start_date": "{{ p_frozen_esql_query_min_date }}",
      "bulk_end_date": "{{ p_frozen_esql_query_max_date }}"
    },
    {
      "operation": "kql_esql_query",
      "clients": 1,
      "warmup-iterations": 20,
      "iterations": 100,
      "tags": ["esql", "search"],
      "bulk_start_date": "{{ p_frozen_esql_query_min_date }}",
      "bulk_end_date": "{{ p_frozen_esql_query_max_date }}"
    },
    {
      "operation": "syslog_qstr_esql_query",
      "clients": 1,
      "warmup-iterations": 20,
      "iterations": 100,
      "tags": ["esql", "search"],
      "bulk_start_date": "{{ p_frozen_esql_query_min_date }}",
      "bulk_end_date": "{{ p_frozen_esql_query_max_date }}"
    }
  ]
}